"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.DirectiveTemplate = void 0;
const shared_1 = require("../../shared");
const TemplateUtils_1 = require("./TemplateUtils");
class DirectiveTemplate {
    static resolve(f) {
        let argsString = '';
        if (f.args.length) {
            argsString = `(\n${f.args
                .map((a) => TemplateUtils_1.TemplateUtils.resolverForConnection(a))
                .map((a) => `\t${a}`)
                .join('\n')}\n)`;
        }
        return `@${(0, shared_1.getTypeName)(f.type.fieldType)}${argsString}`;
    }
}
exports.DirectiveTemplate = DirectiveTemplate;
