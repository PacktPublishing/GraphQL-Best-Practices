import { ParserField } from "../../Models";
export declare class TypeDefinitionsTemplates {
    static resolveExtension: typeof TypeDefinitionsTemplates.resolve;
    static resolveUnionExtension: typeof TypeDefinitionsTemplates.resolve;
    static extendedDefinitionTemplate: ({ description, name, data, }: Pick<ParserField, 'description' | 'name' | 'data'>) => string;
    static definitionTemplate: ({ description, name, data, }: Pick<ParserField, 'description' | 'name' | 'data'>) => string;
    static resolve: ({ name, description, interfaces, args, directives, data }: ParserField) => string;
    static resolveDirective: ({ name, description, type, args }: ParserField) => string;
    static resolveUnion: ({ name, description, args, directives, data }: ParserField) => string;
}
