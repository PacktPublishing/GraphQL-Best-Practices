"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.SchemaExtensionTemplate = exports.SchemaDefinitionTemplate = void 0;
const TemplateUtils_1 = require("./TemplateUtils");
const shared_1 = require("../../shared");
class SchemaDefinitionTemplate {
    static resolve(f) {
        return `schema${TemplateUtils_1.TemplateUtils.resolveDirectives(f.directives)}{${f.args
            .map((a) => `\n\t${a.name}: ${(0, shared_1.getTypeName)(a.type.fieldType)}`)
            .join('')}\n}`;
    }
}
exports.SchemaDefinitionTemplate = SchemaDefinitionTemplate;
class SchemaExtensionTemplate {
    static resolve(f) {
        return `extend schema${TemplateUtils_1.TemplateUtils.resolveDirectives(f.directives)}${f.args.length ? `{${f.args.map((a) => `\n\t${a.name}: ${(0, shared_1.getTypeName)(a.type.fieldType)}`).join('')}\n}` : ''}`;
    }
}
exports.SchemaExtensionTemplate = SchemaExtensionTemplate;
